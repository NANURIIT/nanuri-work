<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nanuri.work.com.login.mapper.LoginMapper">

	<!-- 로그인 회원정보 -->
	<select id="getLoginUserDetailsVO" parameterType="String" resultType="com.nanuri.work.com.login.vo.UserDetailsVO">
		SELECT user_id				/* ID */
		     , user_name			/* 이름 */
		     , user_phone_no		/* 휴대전화번호 */
		     , user_password		/* 비밀번호 */
		     , account_activ_yn		/* 계정활성화여부 */
		  FROM tb_user_mm
		 WHERE user_id = #{userId}
	</select>

</mapper>